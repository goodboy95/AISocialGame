# Default to same-origin paths when serving the frontend through the nginx gateway.
# Override with absolute URLs (e.g. http://localhost:8100/api) when running the Vite dev server directly.
VITE_API_BASE_URL=/api
VITE_WS_BASE_URL=/api/ws
